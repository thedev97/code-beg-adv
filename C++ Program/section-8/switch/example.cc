#include <iostream>
using namespace std;

int main()
{
    int n = 5;
    while (n)
    {
        switch (n)
        {
        case 5: cout<<n<<endl;
            break;
        default:
            break;
        }
        exit(0);
    }
}
